@model List<Category>
<ul class="list-unstyled mb-0">
    <li>
        @Html.ActionLink("All Posts","Index","Home")
    </li>
    @foreach (var item in Model)
    {
        <li>
            <a href="@Url.Action("Index","Home",new { cid = item.Id})">@item.CategoryName</a>
        </li>

    }
</ul>